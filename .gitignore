node_modules/

# Local scratch output from `npm run mock` / `npm run demo`
.mock-store/

# Playwright output, if you clone this next to a suite
playwright-report/
test-results/
allure-results/
allure-report/

# The generated report when you run against a real store.
# demo/report.html is deliberately NOT ignored - it is the showcase, and it is
# regenerated by `npm run demo`.
triage-report/

.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
